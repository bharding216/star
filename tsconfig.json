{
    "compilerOptions": {
      /* Language and Environment */
      "lib": ["es2017", "dom"],                                /* Specify a set of bundled library declaration files that describe the target runtime environment. */
      "target": "es2017",                                      /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
      "types": ["node", "jquery"],                            /* Specify type definition files to be included in compilation. */
      "typeRoots": ["node_modules/@types"],                   /* Specify multiple folders that act like `./node_modules/@types`. */
  
      /* Modules */
      "module": "es2015",                                     /* Specify what module code is generated. */
      "moduleResolution": "node",                             /* Specify how TypeScript looks up a file from a given module specifier. */
      "rootDir": "./project/typescript",                      /* Specify the root folder within your source files. */
      "explainFiles": true,                                   /* Print names of files read during the compilation. */
      
      /* Emit */
      "outDir": "project/static/js",                        /* Specify an output folder for all emitted files. */
      "sourceMap": true,                                      /* Generate corresponding .map file. */
  
      /* Interop Constraints */
      "esModuleInterop": true,                                /* Emit additional JavaScript to ease support for importing CommonJS modules. */
      "forceConsistentCasingInFileNames": true,              /* Ensure that casing is correct in imports. */
  
      /* Type Checking */
      "strict": true,                                         /* Enable all strict type-checking options. */
      "noImplicitAny": true,                                  /* Raise error on expressions and declarations with an implied 'any' type. */
      "strictNullChecks": true,                               /* Enable strict null checks. */
  
      /* Completeness */
      "skipLibCheck": true,                                   /* Skip type checking all .d.ts files. */
      "allowJs": true,                                        /* Allow JavaScript files to be a part of your program. */
      "checkJs": false                                        /* Report errors in .js files. */
    },
    "include": ["project/typescript/**/*.ts"],               /* Which files are included in the compilation. */
    "exclude": ["node_modules"]                              /* Which files are excluded from the compilation. */
}
  